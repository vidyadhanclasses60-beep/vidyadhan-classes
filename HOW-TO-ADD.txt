VIDYADHAN CLASSES — extra mobile hardening (OPTIONAL)
=====================================================
FIRST: test vidyadhanclasses.com/contact in an INCOGNITO tab on your phone.
If it already looks correct there, the earlier problem was just browser CACHE
and you DO NOT need this zip.

If incognito STILL shows a problem, then push this:
  app/globals.css            -> overwrite
  app/contact/page.tsx       -> overwrite
Then Commit -> Push, and send me a FRESH video (with the current time on it).

What this adds: overflow-x: clip on <html> too, and the map limited to the
screen width (max-w-full) — extra protection against any sideways scroll.

VIDYADHAN CLASSES — update 9 (SELF-CONTAINED — use this, skip update 8)
======================================================================
Same as update 8, but the 25 topper photos are cropped EVEN TIGHTER so the
poster frame and "congratulations" text are fully gone — just the face shows.

Copy into your cloned GitHub folder (keep folder names), overwrite, then
GitHub Desktop: Commit -> Push.

  - all code folders (app, components, constants, lib, tailwind.config.ts)
  - public/images/  -> overwrite the whole folder (IMPORTANT: replaces the old
    topper photos with the clean tighter ones)

This one package brings the whole site to the final state (updates 4 through 9).

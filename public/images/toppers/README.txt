Drop individual topper photographs here, named after the student
(e.g. divya-sonawane.jpg), then swap the initials avatar in
components/shared/TopperCard.tsx for a <Image> tag pointing at the file.

The site works perfectly without these — it shows a branded
initials avatar as a graceful fallback.
